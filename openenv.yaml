tasks:
  - id: easy
    grader:
      type: scalar
      evaluation: |
        m = observation["metrics"]
        return float(
          (m["expected_profit"] + m["public_sentiment"] + (1 - m["legal_risk"])) / 3
        )

  - id: medium
    grader:
      type: scalar
      evaluation: |
        m = observation["metrics"]
        return float(
          (m["expected_profit"] + (1 - m["legal_risk"]) + m["public_sentiment"]) / 3
        )

  - id: hard
    grader:
      type: scalar
      evaluation: |
        m = observation["metrics"]
        return float(
          (m["expected_profit"] + (1 - m["env_impact"]) + (1 - m["legal_risk"])) / 3
        )
